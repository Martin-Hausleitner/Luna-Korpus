# Preserved parallel delivery

While the Aster-originals assignment was running, main advanced to a separate standalone parametric cabinet application. That work was preserved, not deleted from history.

Previous main commit: `958c2d2c8f3435e0fc19d04d11aad8709484b269`

Archive: https://github.com/Martin-Hausleitner/Luna-Korpus/tree/archive/pre-aster-originals-958c2d2c8f34

This delivery restores the architecture explicitly requested for this assignment: actual Aster Windows desktop, original upstream applications, Luna theme and German launcher labels. The separate cabinet generator is not used as the shell and none of its CSS is used. Publication retains the prior commits as ancestors, without force-pushing.

## Active delivery branch

Concurrent writers continued updating main during publication. To avoid overwriting them, the verified Aster build is published on `release/aster-originals-20260914`, selected as the repository default branch and GitHub Pages source. `main` is left intact. The release tag and archive provide an immutable download independent of concurrent branches.
